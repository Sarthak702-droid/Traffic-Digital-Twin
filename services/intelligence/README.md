# Intelligence boundary

Python 3.12+, NumPy/SciPy for future forecasting and optimization. Epic 1 provides the gRPC interface and state validation. Predict returns UNIMPLEMENTED until forecasting is implemented. This service never persists operator decisions or receives browser requests.

Run from root: `PYTHONPATH=.:packages/contracts/gen/python .venv/bin/python -m services.shared.server intelligence --port 50052`.
