{
  "name": "traffic-digital-twin",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "start": "if [ -x .venv/bin/python ]; then .venv/bin/python scripts/start-all.py; else python3 scripts/start-all.py; fi",
    "dev": "if [ -x .venv/bin/python ]; then .venv/bin/python scripts/start-all.py; else python3 scripts/start-all.py; fi",
    "dev:all": "if [ -x .venv/bin/python ]; then .venv/bin/python scripts/start-all.py; else python3 scripts/start-all.py; fi",
    "dev:web": "npm run dev -w apps/web",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "build:bin": "mkdir -p bin && GOCACHE=${GOCACHE:-/tmp/traffic-go-build-cache} go build -o bin/api ./apps/api/cmd/api",
    "build": "npm run build -w apps/web",
    "typecheck": "npm run typecheck -w apps/web",
    "test": "npm run test:ui && npm run test:python && npm run test:go",
    "test:all": "npm run test:ui && npm run test:python && npm run test:go",
    "test:ui": "npm run test -w apps/web",
    "test:python": "if [ -x .venv/bin/pytest ]; then .venv/bin/pytest; else pytest; fi",
    "test:go": "(cd apps/api && go test ./...)",
    "dashboard": "node server.mjs"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "6.1.1",
    "prettier": "3.6.2"
  }
}
